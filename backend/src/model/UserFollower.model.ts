import { User } from './User';
import { handleErrorsFromDb } from './../config/DbHandles';
import * as jwt from 'jsonwebtoken';

export class UserFollowerModel {
    public save(callback) {
        //Função de seguir um doador na aplicação
    }
}